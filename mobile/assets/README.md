# App assets

Included for EAS / App Store builds:

- `icon.png` — 1024×1024 app icon (dark `#09090b` with “R” mark)
- `splash.png` — splash screen (1284×2778)

Referenced in [`app.config.js`](../app.config.js). Replace with final brand artwork before production release.

Expo Go development does not require custom assets.
